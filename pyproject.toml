[project]
name = "subtus-atlas"
version = "0.1.0"
description = "Mission planning and flight evidence core for SUBTUS"
requires-python = ">=3.11"

[project.scripts]
atlas = "atlas.cli:main"

[tool.pyright]
include = ["atlas", "tests"]

